# Core pose / CV
ultralytics>=8.2.0        # YOLO-Pose (detection + 17-keypoint pose estimation + ByteTrack/BoT-SORT)
opencv-python>=4.9.0      # video I/O, drawing, evidence capture
numpy>=1.26.0

# Dashboard / analytics
streamlit>=1.34.0
pandas>=2.2.0
plotly>=5.22.0

# Testing
pytest>=8.0.0

# Notes:
# - torch/torchvision are pulled in automatically by ultralytics.
# - SQLite ships with the Python standard library (sqlite3), no extra package needed.
# - For webcam/RTSP support, OpenCV's build must include the relevant backend
#   (ffmpeg for RTSP); the standard opencv-python wheel includes this on
#   Windows/Linux/macOS.
